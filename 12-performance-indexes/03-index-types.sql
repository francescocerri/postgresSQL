-- type of index

-- B-tree -> 99% quello che ci serve, il sistema che abbiamo spiegato prima
-- hash -> più veloce per le uguaglianze
-- GiST -> geometry, full text serach
-- SP- GiST -> data clusters
-- GIN -> colonne che contengono array o JSON
-- BRIN -> database enormi